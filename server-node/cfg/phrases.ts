export namespace Phrases {
    export enum Administrator {
        LVL_TOO_HIGH = "The level is higher than max level!",
        LVL_TOO_LOW = "The level is lower the zero!"
    }

    export enum General {
        UNKNOWN_ERROR_OCCURRED = "Unknown error occurred."
    }

    export enum Db {
        NO_RECORD_FOUND = "No records matching the query found.",
        CONNECTION_PROBLEM = "There is currently a connection problem with the database."
    }
}